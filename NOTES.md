# ملاحظات الموقع — ساحة (saaha.net)

آخر تحديث: 2026-09-20 · الأقسام الثلاثة: الإعلانات (الجذر) · السياحة (`tourism/`) · ملعبك (`malaab/`).
هالملف للمالك: شو انعمل، شو لازم يتعمل يدوياً، وكيف تقرأ الأرقام. التفاصيل التقنية بسجل git.

## ✅ اللي انعمل بأيلول 2026

### القياس والتسويق
- **Meta Pixel + Conversions API** شغّالين ومتحقّق منهم (بكسل `1875215833859027`، الدالة `netlify/functions/meta-capi.js`).
  المسار: PageView → ViewContent → Search → CompleteRegistration → Lead (إضافة محتوى) → Contact (اتصال/واتساب/اتجاهات).
- **Google Analytics** (`G-Y4216697RK`) وMeta مربوطين ببانر الكوكيز: ما بيشتغل شي قبل الموافقة.
- **إحصائيات داخلية** (`js/site-stats.js` بكل صفحة → جدول `site_events`): كل الزيارات من كل الأقسام،
  بلا IP ولا بيانات شخصية. بتظهر بلوحة الأدمن → التحليلات.

### السيو
- الرسم على السيرفر (SSR) لصفحات: `ads.html`، `listing.html`، `tourism/place.html`، وصفحات تفاصيل ملعبك السبعة.
  جوجل بيشوف العنوان والوصف والمحتوى وJSON-LD بلا JS. الروابط الغلط بترجع 404 حقيقي.
- canonical + sitemap + تحويل 301 للروابط القديمة `/?cat=…` → `/ads.html?cat=…`.

### السرعة والأخطاء
- الصور من 22MB → 2.5MB، السكربتات بآخر الصفحة، Leaflet غير معطّل، Service Worker بيجيب JS/CSS من الشبكة أولاً
  (كان يعرض صفحة مكسورة لتحميلة وحدة بعد كل نشر).

### التسجيل (سبب انسحاب الزوار)
- بانر الكوكيز كان ياكل **نص شاشة الجوال** ويغطّي نموذج الدخول وزر النشر — صار ~140px.
- **الدخول عبر جوجل** كان يفشل بصمت (One Tap/FedCM) — صار زر جوجل الرسمي بنافذة اختيار حساب.
- الزائر الجاي من "نشر الإعلان" صار يوصل على تبويب **كضيف** (اسم + جوال) مع رسالة "إعلانك محفوظ — خطوة أخيرة".

### لوحة الأدمن (`admin.html`)
- تحكم كامل بكل الأقسام: بانتظار المراجعة / منشور / مرفوض، موافقة ونشر، رفض، إخفاء وإرجاع للمراجعة، حذف نهائي،
  تعديل الأماكن السياحية، نقل الإعلان لقسم/مدينة، تجديد نشر الإعلان، حذف التعليقات والتقييمات، حذف الحسابات.
- **التحليلات**: الزوار (من وين وكيف) · مسار التحويل (وين بيوقفوا) · أخطاء وأداء (شو بيكسر عند الزوار).

### تنظيف ومواءمة (2026-09-20)
- **sitemap.xml** صار يشمل السياحة وملعبك: صفحات الأقسام، المعالم الخمسة، وكل مكان/عنصر معتمد (بيطلع تلقائياً عند الموافقة).
- **robots.txt + noindex** لصفحات الدخول/التسجيل/الملف الشخصي وأداة البذر وروابط الدوال المباشرة.
- انحذف الكود القديم اللي ما عاد إله قارئ: إحصائيات الزوار المحلية وسجل الأخطاء بـ`js/app.js`، و`malaab/store.js` (مخزن localStorage من أيام المعاينة).
- ملفات التوثيق بـ`malaab/` و`tourism/` (PRE-LAUNCH / PROGRESS / NEXT-STEPS) صارت تصف الوضع الحالي بدل المعاينة القديمة.
- **تكرار المعالم بالسياحة**: المعالم الخمسة موجودة ثابتة (slug) ومبذورة بـSupabase (uuid) — بتظهر مكرّرة أحياناً بالقوائم. الأنظف حذف النسخ المبذورة من اللوحة (السياحة → منشور على الموقع → حذف نهائي)؛ الـsitemap أصلاً بيستثنيها.

## 📊 كيف تقرأ التحليلات
- **الزوار**: الزيارة = جلسة تبويب واحدة. "غادروا من أول صفحة" العالي = صفحة الهبوط ما بتقنع.
  جدول "المصادر والحملات" بيقارن كل مصدر: زيارات، مدة، تواصل، حسابات، نشر — هاد اللي بيحدّد وين تحط الميزانية.
- **مسار التحويل**: كل خطوة = كم زيارة وصلتها، ووين الانسحاب. "بدأ يكتب" و"ضغط نشر" بتنسجّل تلقائياً من نماذج الإضافة.
  "نية التواصل" = ضغطات اتصال/واتساب/اتجاهات بكل محتوى — أهم إشارة إن المحتوى بيشتغل.
- **أخطاء وأداء**: أخطاء JavaScript اللي صارت فعلاً عند الزوار (بصفحة ومتصفّح مين)، وسرعة كل صفحة كما حمّلت عندهم.
- "ملاحظات جاهزة" فوق كل شاشة: قواعد بسيطة على الأرقام، بتطلع بس لمّا البيانات تكفي.

## 📣 ملاحظة للحملات (لمّا ترجع تشغّلها)
ضيف على رابط كل إعلان: `?utm_source=facebook&utm_medium=cpc&utm_campaign=اسم-الحملة`
(مثال: `https://saaha.net/ads.html?cat=cars&utm_source=facebook&utm_medium=cpc&utm_campaign=cars-damascus-oct`).
بتشوف بعدها بجدول "المصادر والحملات" أي حملة جابت مين، وشو عمل (تواصل / حساب / نشر) — مو بس كم زيارة.
بدون utm بيظهر المصدر كـ`facebook.com` أو `l.facebook.com` بلا تمييز بين الحملات.

## 🔧 SQL اللي انشغّل بـSupabase (SQL Editor) — كله موجود بـ`supabase_schema.sql`
- سياسات لوحة الأدمن: حذف الأماكن السياحية، قراءة/تعديل كل الحجوزات.
- جدول `site_events` + سياساته (إدخال للكل، قراءة/حذف للمشرف).
- توسيع أنواع `site_events` لتشمل `step` و`error` (ينشغّل بعد نشر تحديث التحليلات).

## 🚧 لسا معلّق (يدوي عند المالك)
- Netlify: متغيّر `SUPABASE_SERVICE_ROLE_KEY` لتفعيل حذف الحسابات من اللوحة (`netlify/functions/admin-delete-user.js`).
- Meta Events Manager: ترتيب أولوية أحداث iOS (Lead أولاً)، وتسمية مجموعة البيانات.
- Search Console: إعادة إرسال sitemap بعد التحديثات.
- ملعبك: الجداول لسا فاضية (ملعب "النجوم" التجريبي رجع مرفوض) — أول ملاعب حقيقية لازم تنضاف وتعتمد.

## 📌 ثوابت
- كل دفعة لـGitHub = نشر على Netlify (15 كريدت) — منجمّع التعديلات وندفع مرة وحدة.
- المفاتيح السرية (توكن Meta، service_role) بمتغيّرات Netlify بس — ما بتنحط بالمستودع أبداً.
- النسخ الاحتياطية بـ`D:\WORK\نقطة-الاستعادة` — ما تنلمس بلا سؤال.
